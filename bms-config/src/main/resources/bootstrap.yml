server:
  port: 8002

spring:
  application:
    name: bms-config
  security:
    user:
      name: plei
      password: plei
  rabbitmq:
    host: 192.168.3.128
    port: 5672
    username: plei
    password: plei
  cloud:
    config:
      server:
        git:
          #配置git仓库的地址
          uri: https://github.com/pleier/bms-cloud-config.git
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://yangl:yangl@localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: bus-refresh